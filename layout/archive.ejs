<% page.posts.each(function(item){ %>
  <%- partial('_partial/archive', {item: item, index: true,desc: true}) %>
<% }); %>
<% if (page.total > 1){ %>
  <nav id="page-nav" class="clearfix<% if (theme.index.expand != true) { %> unexpand<% } %>">
    <%- paginator({
      prev_text: '<span></span>Prev',
      next_text: 'Next<span></span>'
    }) %>
  </nav>
<% } %>